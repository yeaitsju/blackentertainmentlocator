{
  "name": "api-project",
  "private": true,
  "version": "0.0.0",
  "homepage": ".",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "prettier -c .",
    "fix": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^2.7.1",
    "vite": "^3.2.0"
  },
  "dependencies": {
    "leaflet-providers": "^1.13.0"
  }
}
